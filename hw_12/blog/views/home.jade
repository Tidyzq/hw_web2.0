extends header

block content
	#main_content.detail(ng-controller='homeCtrl')
		h1 Home
		.posts(ng-repeat='post in posts')
			h2 {{post.title}}
			p {{post.time}}
			p {{post.author.name}}
		button#newpost(name='newpost', ng-click='newPost()') New Post